language: php
php:
  - '5.4'
  - '5.5'
  - '5.6'
  - '7.0'

before_script:
  - rm composer.lock
  - composer self-update
  - composer install --prefer-source --no-interaction --dev

script: php vendor/bin/phpunit "$@" --configuration tests/phpunit_coverage.xml ./tests
